## Deploying Latest Changes

1. **Push your latest code:**
  ```bash
  git add .
  git commit -m "Update for latest changes"
  git push
  ```

2. **Deploy using npm:**
  ```bash
  npm run deploy
  ```